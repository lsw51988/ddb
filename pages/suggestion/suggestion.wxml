<view class="title">感谢您真诚的提出意见，我们一定认真考量,优化系统.</view>
<form bindsubmit="formSubmit">
    <view class='s_type'>
        类别：
        <checkbox-group bindchange="checkboxChange">
            <label wx:for='{{checkboxItems}}' wx:key="{{value}}">
                <checkbox value='{{item.value}}' name="type">{{item.name}}</checkbox>
            </label>
        </checkbox-group>
    </view>
    <view class='s_type'>
        具体描述：
        <textarea name="content"></textarea>
    </view>
    <button formType="submit">提交</button>
</form>